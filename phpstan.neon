parameters:
  level: 5
  paths:
    - lib
  excludePaths:
    - lib/AppInfo/Application.php
  ignoreErrors:
    # SuiteCRM v8 REST responses are dynamic JSON, so we accept typed arrays.
    - '#Cannot access offset .* on array\|string#'
